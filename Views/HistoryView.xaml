<UserControl x:Class="EasyGet.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             Loaded="HistoryView_Loaded">

    <Grid Margin="40,32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="历史记录" FontSize="28" FontWeight="ExtraBold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   FontFamily="Segoe UI Variable, Microsoft YaHei UI"
                   Margin="0,0,0,24"/>

        <!-- 搜索 + 清空 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Style="{StaticResource DarkTextBox}"
                     Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1" Style="{StaticResource SurfaceButton}"
                    Content="🔍 搜索" Margin="8,0,0,0"
                    Command="{Binding SearchCommand}"/>
            <Button Grid.Column="2" Style="{StaticResource SurfaceButton}"
                    Content="🗑 清空" Margin="8,0,0,0"
                    Command="{Binding ClearAllCommand}"/>
        </Grid>

        <!-- 历史列表 -->
        <ListBox Grid.Row="2" ItemsSource="{Binding HistoryItems}"
                 Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource BgSurfaceBrush}" CornerRadius="10"
                            BorderBrush="{StaticResource BorderSubtleBrush}" BorderThickness="1"
                            Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="4"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- 平台色条 -->
                            <Border Grid.Column="0" CornerRadius="2" Width="4"
                                    Background="{Binding Platform, Converter={StaticResource PlatformToColor}}"/>

                            <!-- 内容 -->
                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding Platform}" FontSize="11"
                                               Foreground="{StaticResource AccentBrush}" FontWeight="Medium"/>
                                    <TextBlock Text="{Binding Format, StringFormat=' · {0}'}" FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding FileSizeText, StringFormat=' · {0}', Mode=OneWay}" FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"/>
                                    <TextBlock Text="{Binding DownloadTimeText, StringFormat=' · {0}', Mode=OneWay}" FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- 操作 -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource SurfaceButton}" Content="📂"
                                        ToolTip="打开文件夹" Padding="8,5"
                                        Command="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding FilePath}"/>
                                <Button Style="{StaticResource SurfaceButton}" Content="🗑"
                                        ToolTip="删除记录" Padding="8,5" Margin="4,0,0,0"
                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding Id}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
